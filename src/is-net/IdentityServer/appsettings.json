{
  // Passkey (WebAuthn / FIDO2) configuration.
  // Copy the "identityserver" section below into your environment-specific
  // settings file (e.g. _config/default.identityserver.net.json) and
  // adjust the values for your deployment.
  //
  // "identityserver": {
  //   "Login": {
  //     "Passkey": {
  //       // Enable passwordless first-factor sign-in via passkey.
  //       "AllowPasswordless": "true",
  //       // Enable passkey as a second factor after username/password.
  //       "AllowSecondFactor": "false",
  //       // WebAuthn Relying Party ID — must be the effective domain of
  //       // your deployment (e.g. "example.com"). Required when passkeys
  //       // are enabled; leave blank to inherit the request host.
  //       "ServerDomain": "",
  //       // Human-readable name shown in the authenticator during
  //       // passkey registration.
  //       "RelyingPartyName": "IdentityServer"
  //     }
  //   }
  // }
}
